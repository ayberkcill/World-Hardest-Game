using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Insan : Memeliler
{
    
    
    protected override void Dogur()
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        Run();
        beslen();
        Dogur();    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
